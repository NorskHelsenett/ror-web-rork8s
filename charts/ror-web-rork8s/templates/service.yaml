apiVersion: v1
kind: Service
metadata:
  name: {{ include "ror-web-rork8s.fullname" . }}
  labels:
    {{- include "ror-web-rork8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ror-web-rork8s.selectorLabels" . | nindent 4 }}
